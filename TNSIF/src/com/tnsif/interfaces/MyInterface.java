package com.tnsif.interfaces;

public interface MyInterface {
	int min=10; //constant (static and final)
	void display(); // methods always automatically abstract
    //We cannot have constructor or a method with implementation
}
